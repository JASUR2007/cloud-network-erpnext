name: erpnext-crm

services:
  crm-demo:
    build:
      context: .
      dockerfile: deploy/Dockerfile.crm-demo
    ports:
      - "3000:3000"
    restart: unless-stopped

  db:
    image: mariadb:10.6
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --skip-character-set-client-handshake
      - --max-allowed-packet=256M
    environment:
      MYSQL_ROOT_PASSWORD: erpnext
    ports:
      - "3307:3306"
    volumes:
      - db-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-perpnext"]
      start_period: 30s
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    restart: unless-stopped

  adminer:
    image: adminer:latest
    ports:
      - "8088:8080"
    environment:
      ADMINER_DEFAULT_SERVER: db
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  erpnext:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DB_HOST: db
      DB_PORT: "3306"
      REDIS_CACHE: redis
      REDIS_QUEUE: redis
      SITE_NAME: erpnext.local
      DB_PASSWORD: erpnext
      ADMIN_PASSWORD: admin
    ports:
      - "80:8000"
    volumes:
      - sites-vol:/home/frappe/frappe-bench/sites
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "bash", "-c", "exec 3<>/dev/tcp/127.0.0.1/8000 && echo -e 'GET /api/method/frappe.ping HTTP/1.1\r\nHost: erpnext.local\r\nConnection: close\r\n\r\n' >&3 && cat <&3 | head -1 | grep -q 200"]
      interval: 30s
      timeout: 10s
      retries: 6
      start_period: 120s
    restart: unless-stopped

volumes:
  db-data:
  sites-vol:
